<%= form_for(buket) do |f| %>
  <% if buket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buket.errors.count, "error") %> prohibited this buket from being saved:</h2>

      <ul>
      <% buket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
